
/**
 * Renkleri CMYK formatında tanımlar.
 * Değerler 0 ile 100 arasındadır.
 */
export type CMYKColor = {
  c: number; // Cyan
  m: number; // Magenta
  y: number; // Yellow
  k: number; // Key (Black)
};
